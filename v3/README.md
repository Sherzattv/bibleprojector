# Bible Projector v3

Основная и единственная активная линия разработки Bible Projector на
Svelte 5, TypeScript, Vite и Tailwind CSS 4.

Текущая версия: **3.0.0-preview.1**. Статус preview означает, что ещё не
закрыты релизные требования 3.0.0, но вся дальнейшая разработка ведётся на
этом коде. `app/` используется только как legacy-источник данных и
совместимости.

## Реализовано

- единый Omnibox для точных ссылок, полнотекстового поиска и песен;
- MiniSearch в Web Worker с debounce и защитой от устаревших ответов;
- четыре перевода: RST, NRT, KTB и KYB;
- офлайн-каталог из 11 524 песен;
- Preview/Live, слайды песен и навигация по главам Библии;
- отдельное окно проектора через BroadcastChannel;
- проверка реального подключения окна через ping/pong;
- Window Management API для размещения popup на внешнем экране;
- Wake Lock и полноэкранный режим по двойному клику на экране;
- blackout, заметки и локально сохраняемые правки стихов;
- сохраняемые настройки масштаба и отображения ссылки;
- сохраняемые порядок служения и история эфира;
- перестановка, удаление, очистка, импорт и экспорт порядка;
- миграция порядка из localStorage v2;
- PWA с precache оболочки и версионированным Cache Storage для данных;
- адаптивные выдвижные панели для планшетов и телефонов.
- Playwright E2E критических путей для точной ссылки и многосекционной
  песни: Preview, Live, история и восстановление порядка после перезагрузки.

## Текущие ограничения

- нет параллельного показа двух переводов;
- нельзя выбрать и запомнить конкретный внешний монитор;
- пульт не может запросить fullscreen или закрыть окно проектора;
- размер текста задаётся масштабом, автоматического autofit пока нет;
- кнопка логотипа отключена, дополнительные темы не реализованы;
- в Omnibox нет навигации по результатам стрелками и полной ARIA combobox-модели;
- браузерное покрытие пока ограничено двумя критическими сценариями;
- часть поискового ядра временно переиспользуется из legacy JavaScript;
- `npm audit` сообщает о dev-цепочке `vite-plugin-pwa`; production-зависимости
  проходят `npm audit --omit=dev` без уязвимостей.

Актуальные приоритеты и критерии релиза находятся только в
[`ROADMAP.md`](../ROADMAP.md).

## Требования

- Node.js 22;
- npm;
- исходные данные в `../app/js/data/` для полной локальной сборки;
- Chromium-браузер рекомендуется для Window Management API.

Window Management API зависит от поддержки браузера и разрешения
пользователя. Если API недоступен, приложение открывает обычное popup-окно.

## Локальный запуск

Из каталога `v3/`:

```bash
npm ci
npm run data
npm run dev
```

Vite покажет локальный адрес. Пульт доступен по `/`, экран проектора — по
`/#display`. Обычно экран удобнее открывать кнопкой «Открыть экран», чтобы
сработало размещение на внешнем мониторе.

`npm run data` выполняет:

1. чтение RST, NRT, KTB, KYB и песен из `../app/js/data/`;
2. удаление дублирующихся VerseId и пустых стихов;
3. проверку структуры всех 66 книг;
4. запись полных JSON в `public/data/`;
5. создание контент-хэшированного `manifest.json`;
6. создание небольшого demo-набора.

`public/data/` и `src/lib/demo-data.json` генерируются локально и
игнорируются Git.

## Команды

| Команда | Назначение |
| --- | --- |
| `npm run dev` | Vite dev server |
| `npm run check` | Svelte diagnostics и TypeScript |
| `npm test` | Vitest, однократный прогон |
| `npm run test:watch` | Vitest в watch-режиме |
| `npm run test:e2e` | Данные, production-сборка и Chromium E2E |
| `npm run data` | Конвертация и валидация полных данных |
| `npm run build` | Production PWA в `dist/` |
| `npm run preview` | Просмотр содержимого `dist/` |
| `npm run build:demo` | Один HTML с сокращённым встроенным набором |

Полная локальная проверка:

```bash
npm run check
npm test
npx playwright install chromium
npm run test:e2e
npm audit --omit=dev
```

Установка Chromium нужна один раз на рабочую машину. В CI браузер
устанавливается автоматически с системными зависимостями.

## Архитектура

```text
v3/
├── scripts/
│   ├── convert-data.mjs     # Полная конвертация
│   └── convert-core.mjs     # Очистка, валидация и хэши
├── public/data/             # Генерируемые JSON и manifest
├── e2e/                     # Браузерные сценарии Playwright
├── playwright.config.ts     # Production-preview и Chromium
├── src/
│   ├── lib/components/      # Пульт и DisplayView
│   ├── lib/legacy/          # canonical/search/songs + декларации типов
│   ├── lib/commands.svelte.ts
│   ├── lib/db.svelte.ts
│   ├── lib/show.svelte.ts
│   ├── lib/setlist.svelte.ts
│   ├── lib/history.svelte.ts
│   ├── lib/projector-*.ts
│   ├── lib/search-*.ts      # Worker, клиент и индексы
│   ├── App.svelte           # Рабочее место оператора
│   └── main.ts              # Выбор пульта или #display
└── tests/
```

### Данные и офлайн

RST и песни блокируют только начальную готовность приложения. NRT, KTB и
KYB загружаются в фоне; ошибка одного перевода не роняет пульт.

Оболочка приложения кэшируется Service Worker. JSON-файлы загружаются по
манифесту контент-хэшей и сохраняются в Cache Storage. При обновлении
перекачиваются только изменившиеся данные; при сетевой ошибке допустим
stale-fallback из локального кэша.

### Поиск

UI отправляет запросы через `SearchClient`. Клиент делает debounce, Worker
строит отдельный индекс для каждого загруженного перевода и отбрасывает
устаревшие ответы по sequence number. Точная ссылка обрабатывается до
fuzzy-поиска.

### Эфир и внешний экран

Все пользовательские действия проходят через `commands.svelte.ts`.
`show.svelte.ts` хранит Preview/Live, а `ProjectorLink` передаёт
проекционный DTO и настройки по BroadcastChannel. Экран отправляет
heartbeat, поэтому статус подключения в пульте отражает реальное окно.

Порядок, история, настройки и правки текста сохраняются локально. Серверная
синхронизация и аккаунты отсутствуют.

## Сборка `/next/` и деплой

Из корня репозитория:

```bash
npm run build:v3
```

Команда заново генерирует данные и собирает приложение в `../next/` с
`base=/next/`.

```bash
npm run deploy
```

Команда публикует весь статический проект через Cloudflare Workers Static
Assets. Перенос v3 с `/next/` на основной URL выполняется только после
закрытия критериев релиза из roadmap.

## Правила дальнейшей работы

- новый продуктовый код создаётся только в `v3/`;
- `app/` не расширяется новыми функциями;
- изменения legacy допустимы для данных, миграции или критической
  совместимости опубликованной v2;
- каждое изменение поведения сопровождается тестом и записью в changelog;
- новые задачи добавляются в `ROADMAP.md`, а не в архивные документы.
