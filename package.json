{
    "name": "bibleprojector",
    "version": "3.0.0",
    "description": "Bible Projector v3 desktop PWA",
    "type": "module",
    "engines": {
        "node": ">=22"
    },
    "scripts": {
        "start": "npm run dev --prefix v3",
        "build": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\" && npm ci --prefix v3 && npm run data --prefix v3 && npm run build:app --prefix v3 && node -e \"require('fs').cpSync('site','dist',{recursive:true})\"",
        "build:v3": "npm run build",
        "smoke": "node scripts/smoke-serve.mjs",
        "deploy": "wrangler deploy"
    },
    "devDependencies": {
        "prettier": "^3.0.0",
        "wrangler": "^4.114.0"
    },
    "author": "Sherzat",
    "license": "MIT"
}
